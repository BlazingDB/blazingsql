from setuptools import setup, find_packages

setup(
    name='blazingsql',
    version='0.1',
    author='BlazingDB',
    packages=find_packages(),
    install_requires=(
    ),
    zip_safe=False,
)
